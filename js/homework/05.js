// Homework 05. Functions (parameters, return, helper-functions)
// Мы закрепляем темы из урока 05:
// - функции (function / arrow function)
// - параметры и аргументы
// - return (возврат значения)
// - композиция функций (helper-functions)
// - аккуратная работа без глобальных переменных (side effects)
//
// Постарайся ВСЁ РЕШАТЬ В ЭТОМ ФАЙЛЕ, ПОД КАЖДОЙ ЗАДАЧЕЙ.
// Пиши код, НЕ МЕНЯЙ тексты условий задач.
//
// Важно:
// - Используй const там, где значение НЕ меняется.
// - Для вывода используй console.log(...)
//
//
// Task 1. Mini calculator (функции + return + helper-functions)
// 1. Создай 4 функции (arrow function):
//    - add(a, b) -> возвращает сумму
//    - subtract(a, b) -> возвращает разность a - b
//    - multiply(a, b) -> возвращает произведение
//    - safeDivide(a, b) -> возвращает a / b, но:
//        - если b === 0, функция должна вернуть null
// 2. Создай функцию calculate(a, b, operator), где operator — строка: "+", "-", "*", "/".
//    - функция должна использовать helper-functions из пункта 1
//    - если operator неизвестен, вернуть null
//
// Проверь (обязательно сделай console.log):
// - calculate(10, 5, "+") -> 15
// - calculate(10, 5, "-") -> 5
// - calculate(10, 5, "*") -> 50
// - calculate(10, 5, "/") -> 2
// - calculate(10, 0, "/") -> null
// - calculate(10, 5, "%") -> null
//
//
// Task 2. Quadratic equation solver (дискриминант + корни)
// Квадратное уравнение: a*x^2 + b*x + c = 0
// 1. Создай функцию getDiscriminant(a, b, c), которая возвращает D = b*b - 4*a*c
// 2. Создай функцию getRoots(a, b, c), которая:
//    - считает дискриминант через getDiscriminant(...)
//    - если D < 0 -> возвращает [null, null]
//    - иначе возвращает [x1, x2], где:
//      x1 = (-b + sqrt(D)) / (2*a)
//      x2 = (-b - sqrt(D)) / (2*a)
//
// Проверь (обязательно сделай console.log):
// - getDiscriminant(1, -3, 2) -> 1
// - getRoots(1, -3, 2) -> [2, 1]
// - getRoots(1, 2, 5) -> [null, null]
//
//
// Task 3. No global state (без глобальных переменных)
// В уроке мы видели пример, где функция меняет переменную снаружи (side effect).
// Здесь нужно сделать “чистые” функции, которые НЕ зависят от внешних переменных.
//
// 1. Создай функцию sumNumbers(a, b), которая возвращает сумму a + b (через return).
// 2. Создай функцию multiplySum(a, b, c), которая:
//    - сначала находит сумму a + b (используй sumNumbers)
//    - потом умножает результат на c
//    - возвращает итоговое значение
//
// Проверь (обязательно сделай console.log):
// - sumNumbers(2, 5) -> 7
// - multiplySum(2, 5, 10) -> 70
//
//
// Конец Homework 05. Не забывай запускать файл и проверять вывод в консоли.



